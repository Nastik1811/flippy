const size = {
  xs: '320px',
  sm: '768px',
  m: '1200px',
};

export const device = {
  mobile: `(min-width: ${size.xs})`,
  tablet: `(min-width: ${size.sm})`,
  desktop: `(min-width: ${size.m})`
}

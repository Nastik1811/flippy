const size = {
  xs: '320px',
  sm: '768px',
  lg: '1200px',
};

const device = {
  mobile: `(min-width: 320px)`,
  tablet: `(min-width: 760px)`,
  desktop: `(min-width: 1200px)`
};

export default {size, device};